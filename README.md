# 🏷️ shaba-bot (پیشنهاد نام ریپازیتوری)
رباتی برای تبدیل شماره کارت به شماره شبا و امکانات کمکی دیگر — نوشته‌شده با Python و Telethon

---

## 💡 معرفی
`shaba-bot` یک ربات/اسکریپت تلگرام است که به‌صورت ساده و سریع:
- با دریافت **شماره کارت**، شماره **شبا** مربوطه را برمی‌گرداند؛
- قابلیت **استعلام کد ملی** در گروه: کاربر کد ملی را ارسال می‌کند و با ریپلای دادن آن پیام و نوشتن `ملی`، عملیات استعلام انجام می‌شود؛
- قابلیت **پرسش از هوش مصنوعی**: کاربر پیامش را با علامت `+` آغاز می‌کند و سپس سؤال را می‌پرسد؛ ربات پاسخ تولید شده (در صورت فعال بودن سرویس AI) را ارسال می‌کند.

> ⚠️ توجه: این پروژه صرفاً برای اهداف آموزشی و تمرینی است. ذخیره یا پردازش اطلاعات حساس (شماره کارت، شبا، کد ملی) باید با احتیاط و رعایت قوانین حریم خصوصی انجام شود.

---

## 🧰 تکنولوژی‌ها
- Python (نسخهٔ پیشنهادی: 3.12+)
- Telethon (کتابخانهٔ Telegram client برای پایتون)


---

## 🔧 پیش‌نیازها
- Python 3.10+  
- pip  
- یک حساب تلگرام برای ساخت session (برای Telethon) یا Bot Token (بسته به پیاده‌سازی)  
- (اختیاری) API Key سرویس هوش مصنوعی در صورت فعال‌سازی قابلیت پرسش از AI

---

## ⚙️ نصب و راه‌اندازی (محلی)

1. کلون کردن ریپازیتوری:
```bash
git clone https://github.com/yourusername/shaba-bot.git
cd shaba-bot
